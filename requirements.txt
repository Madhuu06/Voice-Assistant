# ═══════════════════════════════════════════════════════════
#  M.A.Y.A Voice Assistant — Dependencies
# ═══════════════════════════════════════════════════════════

# ── Core Audio ───────────────────────────────────────────
sounddevice
numpy

# ── Speech-to-Text (Whisper) ─────────────────────────────
openai-whisper
torch

# ── Text-to-Speech ───────────────────────────────────────
kokoro-onnx>=0.1.3      # Offline local realistic TTS (primary)
soundfile               # Audio generation for Kokoro
edge-tts                # Microsoft neural voices (fallback)
pyttsx3                 # System TTS (fallback)

# ── LLM (Ollama via OpenAI SDK) ─────────────────────────
openai>=1.0.0           # OpenAI-compatible client for Ollama

# ── Push-to-Talk ─────────────────────────────────────────
keyboard                # Global hotkey detection (F12)

# ── Configuration ────────────────────────────────────────
PyYAML>=6.0

# ── Vision / Screen Awareness ────────────────────────────
Pillow>=9.0.0           # Screenshots
requests                # Ollama vision API calls

# ── System Control ───────────────────────────────────────
psutil>=5.9.0           # System info (CPU, RAM, battery)
pycaw>=20220416         # Windows audio control
comtypes>=1.1.10        # COM interface for pycaw
pyautogui               # Keyboard, mouse, and Spotify media control
pygetwindow             # Window manipulation
pyperclip               # System clipboard manipulation

# ── Optional: Premium TTS ────────────────────────────────
elevenlabs              # ElevenLabs cloud TTS (optional)
pygame                  # Audio playback for ElevenLabs
